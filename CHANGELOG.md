# سجل التغييرات (CHANGELOG)

جميع التغييرات الموثقة لمشروع **YAMD (Yet Another Media Downloader)**.

---

## [1.2.2] - 2026-09-05

### 🐛 إصلاحات النشر والاستضافة (Deployment & Hosting Fixes)
- **إصلاح خطأ `Port scan timeout` على Render.com:**
  - بدء استماع خادم الويب فوراً على المنفذ المحدد (`PORT`) بمجرد إقلاع التطبيق لاجتياز فحص المنافذ في أقل من ثانية واحدة.
  - توفير مسارات فحص الصحة `GET /` و `GET /healthz` تعيد `200 OK`.
  - تحويل تحديث `yt-dlp` التلقائي إلى مهمة خلفية غير معطلة حتى لا يتأخر فتح المنفذ.
  - دعم النمط المزدوج الذكي (Hybrid): في حال عدم وجود `WEBHOOK_URL` يعمل البوت بنظام Long Polling تلقائياً مع بقاء منفذ الويب مفتوحاً لإرضاء بيئة Render المجانية دون أي انهيار.

### 🛡️ الأمان والنسخ الاحتياطي
- تم إنشاء نسخة احتياطية لكود البوت: `bot.py.backup_20260905_portfix`.

---

## [1.2.1] - 2026-09-05

### 🐛 إصلاحات (Bug Fixes)
- **إصلاح خطأ `AssertionError()` عند تحميل TikTok:**
  - تم استبدال تمرير النص الخام `"chrome"` إلى إعدادات `yt-dlp` بالاستدعاء الصحيح عبر كائن `ImpersonateTarget.from_str("chrome")` التابع لمكتبة `yt_dlp.networking.impersonate`.
  - تفادي الانهيار الداخلي لـ `yt-dlp` الناتج عن فحص الصنف `assert isinstance(target, ImpersonateTarget)`.
  - إضافة آلية ارتداد ذكية (Fallback mechanism): إذا فشل استخراج المقطع بوضع المحاكاة، يعيد الكود المحاولة فوراً بدون محاكاة لتفادي أي خطأ شبكي.

### ⚡ تحسينات (Improvements)
- **تعزيز كفاءة واستقرار استخراج TikWM اللحظي:**
  - دعم إرسال طلبات **POST** أولاً ببيانات نموذج مشفرة (`url` و `hd: 1`) لتفادي مشاكل اقتطاع الروابط وتجاوز قيود الاستضافة السحابية.
  - دعم طلب **GET** بتشفير كامل للرابط (`quote(url, safe='')`) كبديل في حال تعذر طلب POST.
  - دعم معالجة الروابط النسبية (`/video/...`) وتحويلها إلى روابط مطلقة.
  - إضافة ترويسة `Referer: https://www.tiktok.com/` لتفادي حظر تنزيل المقاطع من خوادم الوسائط (TikTok CDNs).

### 🛡️ الأمان والنسخ الاحتياطي
- تم إنشاء نسخة احتياطية لكود البوت قبل التعديل: `bot.py.backup_20260905`.
- فحص سلامة بناء الشيفرة وخلوها من أخطاء الصياغة عبر `py_compile`.
